/* globals module, require */
"use strict";

module.exports = function(grunt) {
    require('load-grunt-config')(grunt);
};

//Potential modules to get later
//grunt.loadNpmTasks('grunt-contrib-watch');      //Live editing recompile
//grunt.loadNpmTasks('grunt-wiredep');            //Direct dependencies to bower
//grunt.loadNpmTasks('grunt-contrib-imagemin');   //Minify images
//grunt.loadNpmTasks('grunt-jsdoc');              //Create documentation (see also: ink-docstrap)

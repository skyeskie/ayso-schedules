export default class Region {
    constructor(
        public id: Number,
        public number: Number,
        public name: String,
        public mapFile: String,
        public lat: Number,
        public lon: Number
    ) {}
}
